@model IEnumerable<WebBanThucAnNhanh.Models.TypeOfFastFood>
@{
    ViewData["Title"] = "Quản lý người dùng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml"; // Trỏ về layout admin mới tạo
}
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameTypeOfFastFood)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameTypeOfFastFood)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTypeOfFastFood">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTypeOfFastFood">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTypeOfFastFood">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
